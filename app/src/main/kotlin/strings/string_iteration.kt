package strings

/**
 * String (Loop)
 * As the String is just a CharArray, therefore we can iterate over each characters
 */

fun main(){
    val name = "Alok Singh"

    for(i in name){
        println(i)
    }
}